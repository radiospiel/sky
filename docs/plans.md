# Plans

Pre-built execution plans live in `sky/agents/plans/`. Plans are blueprints for complex,
multi-step tasks. An agent can load a plan from `agents/plans/<name>.md`, adapt it to
the current task, and execute it step by step.
